# Production Requirements - Pinned versions for stability
# Last updated: 2025-11-01

# Core MCP Server Dependencies
mcp>=0.9.0,<1.0.0
httpx>=0.27.0,<0.28.0
python-dotenv>=1.0.0,<2.0.0

# Production Enhancements
psutil>=5.9.0,<6.0.0          # System resource monitoring
aiofiles>=23.2.0,<24.0.0      # Async file operations (for logging)

# Security
cryptography>=41.0.0,<42.0.0  # Secure credential handling
pydantic>=2.5.0,<3.0.0        # Data validation

# Monitoring & Metrics
prometheus-client>=0.19.0,<1.0.0  # Prometheus metrics

# HTTP & Network
urllib3>=2.0.0,<3.0.0         # HTTP client dependency
certifi>=2023.0.0             # SSL certificates

# Optional: For Redis-based rate limiting
redis>=5.0.0,<6.0.0           # Redis client
hiredis>=2.2.0,<3.0.0         # Fast Redis protocol parser

# Development & Testing (optional in prod)
pytest>=8.0.0,<9.0.0
pytest-asyncio>=0.23.0,<1.0.0
pytest-cov>=4.1.0,<5.0.0
black>=24.0.0,<25.0.0
ruff>=0.3.0,<1.0.0
bandit>=1.7.0,<2.0.0
safety>=3.0.0,<4.0.0
